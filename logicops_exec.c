#include "gosh.h"

int logicops_exec(char *cmdstr, Info shell)
{
	(void)cmdstr;
	(void)shell;
	
	/*int ret;
	char *local_cmd

	if (!cmdstr)
		return (NORMAL_FAIL);
	local_cmd = strdup(cmdstr);
	
	free(local_cmd); */
	printf("logicops_exec called\n");
	return (0);
}